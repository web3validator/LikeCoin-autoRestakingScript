# LikeCoin-autoRestakingScript
This script withdraw  the reward and commission after that immediately delegate it to the validator
# Requirements
ubuntu 18.0.4+

Jq 

sudo apt-get update
sudo apt install jq
